# Raspberry Pi

Place the final `main.py` here.

## Setup
```bash
pip3 install pyserial
python3 main.py
```

Document the radar ports, baud rates and runtime configuration used by the final prototype.
